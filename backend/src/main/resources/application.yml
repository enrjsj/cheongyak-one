spring:
  application:
    name: cheongyak-one
  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@//localhost:1521/FREEPDB1}
    username: ${DB_USERNAME:CHEONGYAK_APP}
    password: ${DB_PASSWORD:}
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: never

server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful
  compression:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

app:
  reb-api:
    base-url: ${REB_API_BASE_URL:https://api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1}
    service-key: ${REB_API_KEY:}
    page-size: ${REB_API_PAGE_SIZE:100}
    max-pages: ${REB_API_MAX_PAGES:20}
  notice-sync:
    cron: "${NOTICE_SYNC_CRON:0 10 3 * * *}"
    zone: Asia/Seoul
    run-on-startup: false
    export-path: target/reb-sample.json

spring.lifecycle.timeout-per-shutdown-phase: 30s
